return {
  "nvim-treesitter/nvim-treesitter",
  opts = {
    highlight = {
      -- enable/disable for all
      enable = false,
      -- disable for specific languages
      -- disable = { 'markdown' }
    },
  },
}
